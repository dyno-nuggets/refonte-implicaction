version: '3.1'

services:
  db:
    image: mysql
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: 'password'
      MYSQL_DATABASE: 'implicaction'
    ports:
      - "3306:3306"
    expose:
      - "3306"
    volumes:ExperienceController.java
    - my-db:/var/lib/mysql
  adminer:
    image: adminer
    restart: always
    ports:
      - "9001:8080"

volumes:
  my-db:

