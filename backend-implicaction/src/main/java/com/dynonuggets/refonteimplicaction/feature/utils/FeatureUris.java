package com.dynonuggets.refonteimplicaction.feature.utils;

import lombok.NoArgsConstructor;

import static lombok.AccessLevel.PRIVATE;

@NoArgsConstructor(access = PRIVATE)
public class FeatureUris {
    public static final String FEATURE_BASE_URI = "/api/features";
}
